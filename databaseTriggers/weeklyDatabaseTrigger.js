exports = async function(changeEvent) {
    /*
  
      Access a mongodb service:
      const collection = context.services.get("Cluster0").db("Resolution").collection("Resolution");
      try {
        if 
      await collection.updateOne(
        { _id: changeEvent.documentKey._id },
        { 
          $weeklyProgress: 0
        }
      );

    //There should be another check for checking weekly result
    } catch (err) {
      
      console.error("Field update failed", err);
    }
  
    */
  };
  